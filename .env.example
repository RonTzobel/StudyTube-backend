# Copy this file to .env and fill in your actual values.
# Never commit your real .env file to version control.

# --- App ---
APP_NAME=StudyTube
DEBUG=true

# --- Database ---
# Format: postgresql+psycopg://<user>:<password>@<host>:<port>/<database_name>
# The "+psycopg" part tells SQLAlchemy to use psycopg v3 (not psycopg2).
DATABASE_URL=postgresql+psycopg://postgres:yourpassword@localhost:5432/studytube

# --- File uploads ---
UPLOAD_DIR=uploads

# --- Security ---
# Generate a strong secret with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme_generate_a_real_secret_key
ACCESS_TOKEN_EXPIRE_MINUTES=60
