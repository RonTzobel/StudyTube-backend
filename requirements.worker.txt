# ============================================================
# requirements.worker.txt — RQ worker container
# ============================================================
# Installed in Dockerfile.worker. Includes all dependencies
# needed for the video processing pipeline:
#   ffmpeg (system) → OpenAI Whisper API → chunking → sentence-transformers
#
# Transcription uses the OpenAI Whisper API — no local faster-whisper
# or CTranslate2 model is loaded in the worker.
#
# torch is installed separately in Dockerfile.worker with --index-url
# https://download.pytorch.org/whl/cpu to guarantee the CPU-only wheel
# (needed by sentence-transformers for chunk embeddings).
# ============================================================

-r requirements.base.txt

# --- AWS S3 (for downloading videos before ffmpeg processing) ---
boto3

# --- External transcription (OpenAI Whisper API) ---
openai>=1.0.0

# --- Chunk embeddings ---
# sentence-transformers uses torch (CPU wheel, installed first in Dockerfile).
sentence-transformers
