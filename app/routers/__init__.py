# routers/
# Each file defines a FastAPI APIRouter for one area of the application.
# Routers are registered in main.py via app.include_router().
# Keep route handlers thin — delegate all logic to the service layer.
