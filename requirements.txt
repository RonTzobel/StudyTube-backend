# ============================================================
# requirements.txt — convenience pointer for local development
# ============================================================
# In Docker, containers use the split files directly:
#   Dockerfile.backend  → requirements.backend.txt
#   Dockerfile.worker   → requirements.worker.txt
#
# For local venv setup install torch first (CPU-only), then this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# ============================================================

-r requirements.dev.txt
